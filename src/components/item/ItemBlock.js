import "./ItemBlock.css";

function ItemBlock(props) {
  return (
    <div
      className="ItemBlock"
      style={{
        height: `${50}px`,
        width: `${50}px`,
      }}
    ></div>
  );
}

export default ItemBlock;
