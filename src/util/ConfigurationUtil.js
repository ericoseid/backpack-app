export default function getConfiguration() {
  return {
    backpackConfiguration: {
      dimensions: {
        rows: 10,
        columns: 10,
      },
      widthFromCenter: 40,
    },
    itemConfiguration: {},
  };
}
